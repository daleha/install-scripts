This module is a work in progress.

The idea is that the "install" and "setup" modules will be separated out into 
callable modules, that will then eventually be moved out of the getbirch source tree altogether.
